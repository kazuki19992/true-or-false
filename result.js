const problemNum = document.getElementById('problem_num');
const correctNum = document.getElementById('correct');
const comboNum = document.getElementById('last_combo');
const scoreNum = document.getElementById('score');

function readJson() {
    var file = fs.OpenTextFile("result.json", 1);
}